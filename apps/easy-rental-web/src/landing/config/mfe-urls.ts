/** Internal console routes — single-app deployment (no external MFE URLs). */
export const MFE_URLS = {
  client: '/client',
  agency: '/agency',
  organisation: '/organisation',
  admin: '/admin',
};
